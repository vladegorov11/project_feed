<ul class="top-bar-items" style="padding: 0 ;">
  <% @caterories = Category.all%>
  <% @caterories.each do |category|%>
     <li><%= link_to category.name, category_path(category)%></li>
  <%end%>
</ul>
